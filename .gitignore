# EternaLink / EternaLegacy - "프로 버전" 보안 규칙

# 1. 환경 설정 파일 (가장 중요!)
# 모든 비밀 키가 들어있음
.env

# 2. 파이썬 가상 환경
# 수천 개의 라이브러리 파일이므로 제외
.venv/
venv/

# 3. 데이터 및 로그 (개인 정보)
# 고객의 유언장 DB와 로그 파일
data/
logs/
*.db
*.sqlite
*.log
*.bak

# 4. 런타임 파일 및 캐시
__pycache__/
*.pyc
outbox/
report/
recovery/tmp/

# 5. 설정 파일 (백업용)
# .env로 통합했으므로 config 폴더 전체를 제외
config/

stripe_backup_code.txt
